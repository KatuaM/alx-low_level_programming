#include "main.h"

/**
*_isdigit - checks for a digit (0 through 9)
*@c: displays digits
* Return: 1 if character is digit and 0 for otherwise
*/
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
	return (1);
	}
	else
	{
	return (0);
}
	_putchar('\n');
}
